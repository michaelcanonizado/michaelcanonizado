'use client';

import { cn } from '@/lib/utils';
import { ComponentBaseProps } from '@/types';

const InfiniteSlider = ({ className, children }: ComponentBaseProps) => {
  return <div className={cn('', className)}>InfiniteSlider</div>;
};

export default InfiniteSlider;
